# DeltaView

This repository contains the DeltaView Formula 1 data dashboard project.

The actual project files live in the subfolder:

`deltaview-—-formula-1-data-dashboard (2)`

🔗 See the full project README for details, running instructions, and troubleshooting:

- `deltaview-—-formula-1-data-dashboard (2)/README.md`

## Quick start

Option A — change into the project folder (recommended):

```bash
cd "deltaview-—-formula-1-data-dashboard (2)"
npm install
npm run dev -- --host 0.0.0.0
```

Option B — run from the repo root (no cd):

```bash
npm --prefix "deltaview-—-formula-1-data-dashboard (2)" install
npm --prefix "deltaview-—-formula-1-data-dashboard (2)" run dev -- --host 0.0.0.0
```

## Notes

- The dev server runs on port `3000` by default. Use the `--host 0.0.0.0` flag to bind the server to the network if running in a container.
- For build and tests, see the project README linked above.

---
